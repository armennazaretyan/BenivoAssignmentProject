@model IEnumerable<BenivoAssignment.Models.StoryViewModel>

@{
    ViewBag.Title = "Stories";
}


<div class="row">
    <div class="col-lg-12">
        <h3 class="page-header">
            <a href="@Url.Action("Create")" class="btn btn-sm btn-success">Create new Story</a>
        </h3>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    @{
        Html.RenderPartial("_StoryList", Model);
    }
</div>
<!-- /.row -->
